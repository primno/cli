//import { parseConnectionString } from "@tediousjs/connection-string";
import { ODataClient } from "./odata-client/odata-client";

export class Deployer {
    public constructor() {
    }

    public async deploy() {
        try {
            

        }
        catch (except) {
            console.error(except);
        }
    }
}